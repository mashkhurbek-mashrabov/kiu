<?xml version="1.0" encoding="utf-8"?>
<!-- Dark counterpart of kiu_widget_lesson_item: see that file for why the
     stroke carries the boundary now that the fill is neutral. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/widget_item_dark" />
    <stroke android:width="1dp" android:color="@color/widget_outline_dark" />
    <corners android:radius="14dp" />
</shape>
