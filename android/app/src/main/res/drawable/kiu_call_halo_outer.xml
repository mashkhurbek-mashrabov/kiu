<?xml version="1.0" encoding="utf-8"?>
<!-- Second, wider ring. Static: two pulsing rings read as noise, one moving
     against one fixed reads as a signal. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="oval">
    <solid android:color="@android:color/transparent" />
    <stroke
        android:width="1dp"
        android:color="@color/call_halo_ring_faint" />
</shape>
