<?xml version="1.0" encoding="utf-8"?>
<!-- Rounded accent bar. A square-ended rectangle butted against the card's
     14dp corner left a visible notch at the top and bottom of every row. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/accent_scheduled" />
    <corners android:radius="3dp" />
</shape>
